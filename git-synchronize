userid=$(whoami) # Ensure we're running as root

if [[ ("$userid" = "root") ]]; then
	echo "You should not run a git commit as root."
else
	git add -A
	if [ $# -eq 0 ] ; then
		git commit
	else
		git commit -m "$*"
	fi
	git push origin dev
fi

